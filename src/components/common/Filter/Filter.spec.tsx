/**
 * Simple test to test that Jest is working
 */
import React from 'react'
import { render } from '@testing-library/react'

it('should have a checkbox checked when filtering by year', async () => {
  expect(false).toStrictEqual(true)
})
